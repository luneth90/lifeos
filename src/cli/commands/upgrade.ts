import { yellow } from '../utils/ui.js';

export default async function upgrade(_args: string[]): Promise<void> {
	console.log(yellow('lifeos upgrade is not yet implemented. Coming soon.'));
	process.exit(0);
}
