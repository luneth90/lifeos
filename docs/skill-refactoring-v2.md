# LifeOS 技能重构 V2：描述重写 + 角色定义 + 遗留问题

## Context

第一轮重构（commit 1d85a18）完成了共享协议提取、配置块统一、记忆集成去重等结构性改进。本轮聚焦三个方面：

1. **全部 9 个技能的 description 字段重写** — 优化 AI 触发准确率
2. **全部 9 个技能的角色定义（正文第一句）重写**
3. **第一轮遗留的未覆盖问题** — Agent prompt 对齐、长文件拆分等

### 参考：Claude 官方 Skills 指南要点

来源：https://support.claude.com/en/articles/12512198-how-to-create-custom-skills

- **description 是触发决策的核心**：Claude 根据 description 决定是否调用技能
- **明确说明做什么 + 何时使用**：不只是功能描述，还要包含使用场景
- **保持聚焦**：每个技能聚焦一个工作流
- **渐进式披露**：metadata（name + description）→ SKILL.md body → references
- **用例比规则有效**：包含示例输入输出帮助理解

---

## 一、Description 重写

### 当前问题

所有 description 共享一个公式化模板：

```
LifeOS [名称]：[一句话功能描述]。当用户说"[触发词列表]"时触发。不适用于[排除场景]（请用 /[其他技能]）。
```

这个模式存在三个问题：

1. **触发词硬编码** — 列举 5-6 个固定短语，无法覆盖用户实际的多样化表达
2. **功能描述过于抽象** — "将书籍章节或论文结合项目文件与草稿笔记，解析为结构化的知识笔记"不如描述具体使用场景
3. **排除规则过多** — 花大量字数说"不适用于X"，挤压了正面描述的空间

### 重写原则

- **场景导向**：用"当用户想要..."描述使用场景，而非硬编码触发词
- **推式触发**：宁可多触发，不要漏触发（undertrigger 比 overtrigger 代价更高）
- **具体行为**：说明技能产出什么文件、放在哪里
- **简明边界**：排除规则最多一句，放在末尾
- **去"原子"**：全部使用"百科"或"Wiki"替代"原子化"等表述

### 重写方案

#### ask

**当前：**
```
LifeOS 快速问答助手：直接回答问题，不创建计划文件或笔记，按需检索 Vault 已有内容。当用户说"/ask [问题]"、"快速问一下"、"这是什么"、"帮我解释"、"怎么用"时触发。不适用于需要多轮探索的发散问题（请用 /brainstorm），不适用于系统性研究（请用 /research）。
```

**重写为：**
```
快速回答用户问题，按需检索 Vault 已有笔记辅助作答。适用于概念解释、用法查询、Vault 内容检索、PDF 指定页面提问等单轮问答场景。当用户提出任何直接问题或说"/ask"时使用此技能。复杂问题会建议升级到 /brainstorm 或 /research。
```

#### brainstorm

**当前：**
```
LifeOS 交互式头脑风暴：通过多轮对话探索和深化想法，结束后可产出项目、知识笔记或草稿。当用户说"/brainstorm [话题]"、"头脑风暴"、"发散一下"、"我有个想法想聊聊"、"帮我探索这个方向"、"想法还不成熟，聊聊看"时触发。不适用于明确的快速问答（请用 /ask），不适用于已有明确目标的项目创建（请用 /project）。
```

**重写为：**
```
通过多轮交互式对话探索和深化用户的想法，使用 5 Whys、What if、Devil's Advocate 等思维技巧引导发散。结束后可选择创建项目（调用 /project）、整理为百科笔记、或保存为草稿。当用户想聊一个还不成熟的想法、需要发散思维、探索某个方向的可行性、或说"/brainstorm"时使用此技能。
```

#### today

**当前：**
```
LifeOS 晨间规划工作流：回顾昨日完成情况、创建今日日记、连接活跃项目并捕获新想法。当用户说"开始今天"、"今天的计划"、"今天做什么"、"规划今天"、"早安"、"start my day"、"morning planning"、"today"时触发。不适用于快速问答（请用 /ask）。
```

**重写为：**
```
每日规划入口：回顾昨日进展和未完成任务、扫描活跃项目与待复习笔记、收集用户今日目标和新想法、生成今日日记文件。当用户开始新的一天、问"今天做什么"、说"早安"、想规划当日任务、或说"/today"时使用此技能。会自动提示后续可用的技能（/review、/research、/project 等）。
```

#### project

**当前：**
```
LifeOS 项目创建工作流（双 Agent）：将想法、草稿或资源转化为结构化的项目文件，产出到 20_项目/，支持学习/开发/创作/通用四种类型。当用户说"/project [想法]"、"创建项目"、"开始一个新项目"、"把这个想法变成项目"、"我想学习..."、"帮我规划这本书的学习"时触发。不适用于快速问答（请用 /ask）或研究任务（请用 /research）。
```

**重写为：**
```
将想法、草稿或学习资源转化为结构化的项目文件（产出到 {项目目录}/）。使用双 Agent 工作流：规划 Agent 生成计划文件供用户审核，确认后执行 Agent 创建正式项目。支持四种项目类型：学习（章节式规划）、开发（单主项目+文档目录）、创作（里程碑式）、通用。当用户想创建项目、规划一本书的学习、把草稿想法正式化、或说"/project"时使用此技能。
```

#### research

**当前：**
```
LifeOS 深度研究工作流（双 Agent）：将主题或草稿文件研究为结构化报告，仅产出到 30_研究/。当用户说"/research [主题]"、"帮我研究"、"深度调研"、"我想了解"、"给我写一份研究报告"、"深入研究一下"时触发。
```

**重写为：**
```
对指定主题或草稿进行深度研究，产出结构化研究报告到 {研究目录}/。使用双 Agent 工作流：规划 Agent 扫描本地草稿、匹配专家人格、生成研究计划；执行 Agent 结合本地草稿与 WebSearch 外部资料撰写报告。支持主题模式（直接给主题）和文件模式（以草稿为锚点展开）。当用户想深入了解某个主题、需要系统性调研、想把草稿扩展为完整报告、或说"/research"时使用此技能。
```

#### knowledge

**当前：**
```
LifeOS 知识整理技能：将书籍章节或论文结合项目文件与草稿笔记，解析为结构化的知识笔记（笔记/百科），仅产出到 40_知识/。当用户说"/knowledge"、"分析这章知识"、"提取知识点"、"把这章笔记结构化"、"生成百科"、"整理成知识笔记"时触发。需要用户同时提供项目文件和原文内容。不适用于生成研究报告（请用 /research）。
```

**重写为：**
```
从书籍章节或论文中蒸馏结构化知识笔记和百科概念（产出到 {知识目录}/）。需要三类输入：项目文件（必须）、原文内容（必须）、草稿笔记（可选融合）。产出主笔记（按模板结构化）和百科概念（Wiki 条目），并建立双向 Wikilinks。当用户想整理某章知识点、提取百科概念、把原文结构化为笔记、或说"/knowledge"时使用此技能。若无项目文件会提示先用 /project 创建。
```

#### review

**当前：**
```
LifeOS 知识复习工作流：从 40_知识/ 加载已有笔记，生成复习文件供用户在 .md 中作答，完成后触发批改，更新 status 并记录到日记。当用户说"/review"、"复习"、"回顾"、"测一下"、"温故知新"、"检验掌握程度"时触发。当用户说"批改"、"改卷"、"检查复习"时触发批改流程。不适用于新知识整理（请用 /knowledge）。
```

**重写为：**
```
对已有知识笔记进行主动回忆复习。生成复习文件（.md），用户在文件中作答后触发批改，自动更新笔记 status（draft→review→mastered）和项目掌握度。支持三种模式：提问模式（应用题）、费曼模式（用自己的话解释概念）、盲点扫描（自评掌握程度）。当用户想复习、测验掌握程度、说"/review"时使用此技能。用户说"批改"或"改卷"时触发批改流程。
```

#### archive

**当前：**
```
LifeOS 归档工作流：扫描并归档已完成的项目（status:done）和已处理的草稿（status:researched/projected/knowledged），保持 Vault 整洁。当用户说"/archive"、"归档"、"清理"、"整理完成的项目"、"清空已处理草稿"、"整理一下库"时触发。不归档 status:pending 的草稿。
```

**重写为：**
```
扫描并归档已完成的项目（status:done）和已消化的草稿（status:researched/projected/knowledged），按年月移入归档目录并更新 frontmatter。不会触碰 pending 状态的草稿。当用户想清理 Vault、归档已完成的工作、整理库、或说"/archive"时使用此技能。
```

#### read-pdf

**当前：**
```
LifeOS PDF 读取器：从 PDF 书籍或论文中提取文字、图表、公式和表格，产出 JSON 中间成果供下游技能消费。当用户说"读取PDF"、"提取PDF"、"read-pdf"、"解析这本书的第X章"、"把PDF第N页转成笔记素材"时触发。
```

**重写为：**
```
从 PDF 文件中提取文字、图表（Vision 分析）、数学公式（转 LaTeX）和表格（转 Markdown），产出 JSON 中间数据供 /knowledge、/ask、/review 等技能消费。支持页码范围和章节名定位。当用户需要读取 PDF 内容、提取特定页面、解析书籍章节、或说"/read-pdf"时使用此技能。也会被其他技能内部自动调用。
```

---

## 二、角色定义（正文第一句）重写

每个技能的 SKILL.zh.md 正文以角色定义开头，设定 AI 的行为基调。

### 当前角色定义

| 技能 | 当前角色定义 |
|------|------------|
| ask | 你是 LifeOS 的快速问答助手。 |
| brainstorm | 你是 LifeOS 的头脑风暴引导师。 |
| today | 你是 LifeOS 的晨间规划助手。 |
| project | 你是 LifeOS 的项目管理编排专家。 |
| research | 你是 LifeOS 的深度研究编排专家。 |
| knowledge | 你是 LifeOS 的知识蒸馏专家。 |
| review | 你是 LifeOS 的复习教练。 |
| archive | 你是 LifeOS 的归档管理员。 |
| read-pdf | （无角色定义） |

### 问题

- 角色名称过于抽象（"编排专家"、"蒸馏专家"对 AI 触发无帮助）
- 缺少行为预期（角色定义应暗示行为风格）
- read-pdf 没有角色定义

### 重写方案

每个角色定义不只给头衔，还描述**工作方式和行为原则**：

| 技能 | 重写后角色定义 |
|------|-------------|
| ask | 你是 LifeOS 的快速问答助手，擅长用最少的步骤给出最直接的答案。不创建文件、不启动子 Agent、不过度格式化。能从 Vault 已有笔记中找到相关内容时自然引用，找不到时凭知识直接作答。 |
| brainstorm | 你是 LifeOS 的头脑风暴搭档，善于用提问激发思考、用挑战强化想法。你的风格是好奇、支持、有建设性的挑战。在对话中保持探索性，不急于下结论或创建文件，让想法充分发酵后再进入行动阶段。 |
| today | 你是 LifeOS 的每日规划助手，帮助用户快速进入工作状态。你会自动扫描昨日遗留、活跃项目、待复习笔记和草稿池，综合这些信息为用户生成一份可执行的今日计划，减少用户的决策负担。 |
| project | 你是 LifeOS 的项目创建编排者，负责协调规划 Agent 和执行 Agent 将用户的想法转化为结构化项目。你确保每个项目有清晰的分类、合理的章节规划、正确的目录结构，并在用户确认计划后才执行创建。 |
| research | 你是 LifeOS 的深度研究编排者，负责协调规划 Agent 和执行 Agent 完成系统性研究。你确保研究有明确的范围、合适的专家人格、充分利用本地草稿作为第一手资料，并结合外部搜索产出高质量报告。 |
| knowledge | 你是 LifeOS 的知识整理专家，将原文内容重构为高度结构化的知识笔记和百科概念。你严格遵守模板结构和目录约定，确保每篇百科只记一个概念，所有概念通过 Wikilinks 互相关联。 |
| review | 你是 LifeOS 的复习教练，通过主动回忆测试帮助用户巩固已学知识。你出题侧重理解和应用而非死记硬背，批改时既肯定正确部分也指出薄弱环节，并自动维护笔记的掌握度状态。 |
| archive | 你是 LifeOS 的归档管理员，帮助用户保持 Vault 的活跃空间整洁。你只归档已完成的工作，绝不触碰仍在处理中的内容，归档前必须让用户确认清单。 |
| read-pdf | 你是 LifeOS 的 PDF 解析工具，将 PDF 页面转化为结构化的 JSON 中间数据。你通过文字提取和 Vision 图像分析相结合，确保图表、公式和表格都被准确捕获，供下游技能消费。 |

---

## 三、术语统一：去"原子"

全局搜索并替换以下术语：

| 搜索 | 替换为 | 涉及文件 |
|------|--------|---------|
| 原子化 | 独立的百科条目 | brainstorm, knowledge |
| 原子化的概念 | 百科概念 | brainstorm |
| 保持笔记原子化 | 每篇百科只记一个概念 | brainstorm |

---

## 四、第一轮遗留问题

### 问题 B：Agent Reference Prompt 中的配置块冗余

**严重程度：中**

4 个 Agent prompt 文件（project 和 research 各有 planning + execution）各自包含独立的路径配置说明。这些与主 SKILL 文件的配置块功能重复。

**涉及文件：**
- `project/references/planning-agent-prompt.zh.md`
- `project/references/execution-agent-prompt.zh.md`
- `research/references/planning-agent-prompt.zh.md`
- `research/references/execution-agent-prompt.zh.md`

**建议：** Agent prompt 的配置说明简化为一行引用："路径逻辑名由 Orchestrator 从 lifeos.yaml 解析后注入，见主技能文件的配置块。" 因为 Agent 是由 Orchestrator 通过 Task 工具启动的，上下文中已包含路径信息。

### 问题 D：read-pdf 记忆集成异常

**严重程度：低**

第一轮中，read-pdf 被添加了记忆系统集成节（原本没有）。作为一个工具性技能（产出 JSON 中间数据），read-pdf 是否需要 memory_skill_complete 和 session_bridge 值得商榷 — 它通常被其他技能内部调用，不是会话的终点操作。

**建议：** 移除 read-pdf 的记忆集成节，或简化为仅保留 memory_skill_complete（供直接调用场景），移除 session_bridge。

### 问题 E：brainstorm 文件过长（321 行）

**严重程度：低**

brainstorm 是最长的技能文件（321 行），其中包含大量可提取到 references 的内容：
- Phase 1 的头脑风暴技巧详解（5 Whys 等，约 30 行）
- Phase 3 的三个选项完整流程（约 80 行）
- Obsidian 格式规范（约 15 行，与其他技能重复）

**建议：** 将 Phase 3 的三个行动选项提取到 `brainstorm/references/action-options.zh.md`，主文件仅保留选项列表和引用指针。

### 问题 F：review 文件过长（347 行）

**严重程度：低**

review 是第二长的技能文件（347 行），包含完整的出题、批改、状态更新逻辑。

**建议：** 将批改流程（阶段 2.5）和更新逻辑（阶段 3）提取到 `review/references/grading-protocol.zh.md`，主文件保留流程概述和引用。

---

## 五、实施计划

### 第一步：描述重写 + 角色定义 + 术语统一

修改 9 个 SKILL.zh.md 文件：
- frontmatter description 字段重写
- 正文第一句角色定义重写
- "原子"相关术语替换

### 第二步：Agent prompt 精简（问题 B）

精简 4 个 Agent reference prompt 的配置说明。

### 第三步：可选改进（问题 D-F）

- 修复 read-pdf 记忆集成
- 提取 brainstorm 和 review 的长内容到 references

---

## 六、重写后的 Description 速查表

| 技能 | 重写后 description（首句） |
|------|--------------------------|
| ask | 快速回答用户问题，按需检索 Vault 已有笔记辅助作答... |
| brainstorm | 通过多轮交互式对话探索和深化用户的想法... |
| today | 每日规划入口：回顾昨日进展和未完成任务... |
| project | 将想法、草稿或学习资源转化为结构化的项目文件... |
| research | 对指定主题或草稿进行深度研究，产出结构化研究报告... |
| knowledge | 从书籍章节或论文中蒸馏结构化知识笔记和百科概念... |
| review | 对已有知识笔记进行主动回忆复习... |
| archive | 扫描并归档已完成的项目和已消化的草稿... |
| read-pdf | 从 PDF 文件中提取文字、图表、数学公式和表格... |
